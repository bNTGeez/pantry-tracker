'use client'
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
  },
  typography: {
    h1: {
      fontSize: "2.5rem",
      fontWeight: "600",
    },
  },
});

export default theme;
